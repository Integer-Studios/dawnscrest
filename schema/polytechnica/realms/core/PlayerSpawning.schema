package polytechnica.realms.core;

import "polytechnica/realms/core/Nothing.schema";

type SpawnPlayerRequest {
  uint32 player_id = 1;
}

component PlayerSpawning {
  id = 1002;

  command polytechnica.realms.core.Nothing spawn_player(SpawnPlayerRequest);
}
